<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class apartamentos extends Controller
{
    public function calcularprecio(string $area, int $numero_piso = 1) {

        $porcentaje = 1.0;

        for(int $i = 1; $i < $numero_piso; $i++) {

            $porcetaje += 0.01;
        }

        $precio = $area * $porcentaje * 1500;
    }

    return view('calcularprecio', compact('precio'));
}
